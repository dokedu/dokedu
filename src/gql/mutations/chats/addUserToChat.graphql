mutation addUserToChat($input: AddUserToChatInput!) {
    addUserToChat(input: $input) {
        id
        chat {
            id
            users {
                id
                firstName
                firstName
                email
            }
        }
    }
}