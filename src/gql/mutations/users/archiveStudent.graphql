mutation archiveStudent($id: ID!) {
    archiveUser(id: $id) {
        id
        firstName
        lastName
        role
        student {
            birthday
            grade
            leftAt
            joinedAt
        }
    }
}