mutation updateStudent($student: UpdateUserInput!) {
  updateUser(input: $student) {
    id
    firstName
    lastName
    role
    student {
      id
      birthday
      grade
      leftAt
      joinedAt
      emoji
    }
  }
}
