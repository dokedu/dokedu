query emailAccounts {
  emailAccounts(filter: { type: GROUP }) {
    edges {
      id
      name
      description
    }
    totalCount
    pageInfo {
      hasNextPage
      hasPreviousPage
    }
  }
}
