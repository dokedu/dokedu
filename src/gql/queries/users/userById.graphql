query userById($id: ID!) {
    user(id: $id) {
        id
        firstName
        lastName
        student {
            id
            grade
            joinedAt
            leftAt
            entriesCount
            competencesCount
            eventsCount
            emoji
        }
    }
}