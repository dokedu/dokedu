query zeCompetenceParents($id: ID!) {
    competence(id: $id) {
        id
        name
        type
        grades
        parents {
            id
            name
            type
            grades
        }
    }
}