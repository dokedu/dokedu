query chat($id: ID!) {
    chat(id: $id) {
        id
        name
        messages {
            id
            message
            user {
                id
                firstName
                lastName
            }
            createdAt
        }
    }
}