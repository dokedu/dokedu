subscription messageAdded($chatId: ID!) {
    messageAdded(chatId: $chatId) {
        id
        chat {
            id
            lastMessage
        }
        user {
            id
            firstName
            lastName
        }
        message
    }
}