subscription messageAdded {
  messageAdded {
    id
    chat {
      id
      unreadMessageCount
      lastMessage {
        id
        message
      }
    }
    user {
      id
      firstName
      lastName
    }
    message
  }
}
