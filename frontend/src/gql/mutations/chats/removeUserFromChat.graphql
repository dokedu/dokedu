mutation removeUserFromChat($input: RemoveUserFromChatInput!) {
  removeUserFromChat(input: $input) {
    id
    chat {
      id
      users {
        id
        firstName
        lastName
        email
      }
    }
  }
}
