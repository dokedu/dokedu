query files($input: FileFilterInput!) {
    files(input: $input) {
        pageInfo {
            hasNextPage
            hasPreviousPage
        }
        edges {
            ...file
        }
    }
}
